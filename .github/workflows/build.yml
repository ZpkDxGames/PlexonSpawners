name: Build

on:
  push:
    branches:
      - main
      - 'agent/**'
      - 'phase2/**'
      - 'phase3/**'
      - 'release/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plexonspawners-build-${{ github.ref }}
  cancel-in-progress: true

env:
  PLUGIN_VERSION: '3.0.0-rc.2'
  CORE_SHA256: '61d625a717da9f46ee9231e1970d84b4c317ae12cf4090cdf7c9d39b6a1a9baf'

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '9.7.1'

      - name: Provision PlexonCore API 2.0.4
        shell: bash
        run: |
          curl -fsSL -o /tmp/PlexonCore-2.0.4.jar \
            https://github.com/ZpkDxGames/PlexonCore/releases/download/v2.0.4/PlexonCore-2.0.4.jar
          echo "${CORE_SHA256}  /tmp/PlexonCore-2.0.4.jar" | sha256sum --check
          mvn -B install:install-file \
            -Dfile=/tmp/PlexonCore-2.0.4.jar \
            -DgroupId=com.zpkdxgames \
            -DartifactId=PlexonCore \
            -Dversion=2.0.4 \
            -Dpackaging=jar \
            -DgeneratePom=true

      - name: Test and verify PlexonSpawners
        run: gradle clean check --stacktrace

      - name: Verify runtime JAR contract
        shell: bash
        run: |
          JAR="build/libs/PlexonSpawners-${PLUGIN_VERSION}.jar"
          test -f "$JAR"
          jar tf "$JAR" | grep -q '^plugin.yml$'
          jar tf "$JAR" | grep -q '^com/plexon/spawners/PlexonSpawners.class$'
          jar tf "$JAR" | grep -q '^com/plexon/spawners/managed/ManagedSpawnerRegistry.class$'
          jar tf "$JAR" | grep -q '^com/plexon/spawners/managed/SpawnerOriginService.class$'
          jar tf "$JAR" | grep -q '^com/plexon/spawners/gui/SpawnerControlGui.class$'
          if jar tf "$JAR" | grep -q '^com/zpkdxgames/plexoncore/'; then
            echo 'PlexonCore classes were shaded into the plugin JAR.' >&2
            exit 1
          fi
          rm -rf build/release
          mkdir -p build/release
          cp "$JAR" "build/release/PlexonSpawners-${PLUGIN_VERSION}.jar"
          (cd build/release && sha256sum "PlexonSpawners-${PLUGIN_VERSION}.jar") > build/release/SHA256SUMS.txt
          (cd build/release && sha256sum --check SHA256SUMS.txt)

      - name: Check whitespace
        run: git diff --check

      - name: Upload CI artifact
        uses: actions/upload-artifact@v4
        with:
          name: PlexonSpawners-${{ env.PLUGIN_VERSION }}
          path: build/release/
          if-no-files-found: error

  release-candidate:
    if: github.event_name == 'push' && github.ref == 'refs/heads/release/3.0.0-rc.2'
    needs: build
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Checkout candidate commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download verified candidate assets
        uses: actions/download-artifact@v4
        with:
          name: PlexonSpawners-${{ env.PLUGIN_VERSION }}
          path: release-assets

      - name: Verify candidate assets
        shell: bash
        run: |
          test -f "release-assets/PlexonSpawners-${PLUGIN_VERSION}.jar"
          test -f "release-assets/SHA256SUMS.txt"
          (cd release-assets && sha256sum --check SHA256SUMS.txt)

      - name: Publish immutable prerelease
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="v${PLUGIN_VERSION}"
          if git ls-remote --exit-code --tags origin "refs/tags/${TAG}" >/dev/null 2>&1; then
            echo "${TAG} already exists; refusing to move an existing candidate tag." >&2
            exit 1
          fi
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "${TAG} release already exists; refusing to replace candidate assets." >&2
            exit 1
          fi
          git tag "$TAG" "$GITHUB_SHA"
          git push origin "refs/tags/${TAG}"
          gh release create "$TAG" \
            "release-assets/PlexonSpawners-${PLUGIN_VERSION}.jar" \
            "release-assets/SHA256SUMS.txt" \
            --target "$GITHUB_SHA" \
            --title "PlexonSpawners ${PLUGIN_VERSION}" \
            --prerelease \
            --notes-file RELEASE_NOTES.md
